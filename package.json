{
  "name": "cypresse2epomframework",
  "version": "1.0.0",
  "description": "Cypress Automation E2E Framework using POM",
  "main": "index.js",
  "scripts": {
    "test:addToCart": "npx cypress run --spec cypress/e2e/tests/addToCartTest.cy.js",
    "test:registerFlow": "npx cypress run --spec cypress/e2e/tests/registerTest.cy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.2",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
